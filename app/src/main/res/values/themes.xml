<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZandiaKt" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ZandiaKt" parent="Base.Theme.ZandiaKt">
        <!-- Use this theme as the main theme for your application -->
    </style>

    <!-- Theme for the splash screen activity -->
    <style name="Base.Theme.ZandiaLogo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your day/night theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ZandiaLogo" parent="Base.Theme.ZandiaLogo" />

    <style name="Theme.ZandiaLogo.Fullscreen" parent="Theme.ZandiaLogo">
        <item name="android:actionBarStyle">@style/Widget.Theme.ZandiaLogo.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.ZandiaLogo.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/g_green_watermelon</item>
        <item name="fullscreenTextColor">@color/g_red_watermelon</item>
    </style>
</resources>
